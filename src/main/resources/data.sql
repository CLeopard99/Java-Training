CREATE TABLE Accounts (
    Username varchar(255) NOT NULL,
    Password varchar(255) NOT NULL,
    PRIMARY KEY (Username)
);
INSERT INTO Accounts (Username, Password) VALUES ('Account 10', 'password');
INSERT INTO Accounts (Username, Password) VALUES ('Account 11', 'password');

